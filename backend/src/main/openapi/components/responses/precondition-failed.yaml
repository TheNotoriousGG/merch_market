description: The supplied If-Match value does not represent the current resource version.
headers:
  X-Trace-Id:
    $ref: ../headers/trace-id.yaml
content:
  application/problem+json:
    schema:
      $ref: ../schemas/problem-details.yaml
