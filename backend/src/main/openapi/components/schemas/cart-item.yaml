type: object
required: [variantId, productId, slug, name, variantLabel, quantity, unitPriceMinor, lineSubtotalMinor, discountMinor, available]
properties:
  variantId: {type: string, format: uuid}
  productId: {type: string, format: uuid}
  slug: {type: string}
  name: {type: string}
  variantLabel: {type: string}
  quantity: {type: integer, minimum: 1, maximum: 99}
  unitPriceMinor: {type: integer, format: int64, minimum: 1}
  lineSubtotalMinor: {type: integer, format: int64, minimum: 0}
  discountMinor: {type: integer, format: int64, minimum: 0}
  promotionName: {type: string, maxLength: 160}
  available: {type: boolean}
