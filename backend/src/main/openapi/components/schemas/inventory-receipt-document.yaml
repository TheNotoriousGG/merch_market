type: object
required: [documentId, lineCount, totalQuantity, movements]
properties:
  documentId: {type: string}
  lineCount: {type: integer, format: int32, minimum: 1}
  totalQuantity: {type: integer, format: int64, minimum: 1}
  movements:
    type: array
    minItems: 1
    items: {$ref: ./inventory-mutation-result.yaml}
