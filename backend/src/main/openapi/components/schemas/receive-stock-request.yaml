type: object
description: Positive physical receipt into the configured warehouse.
required: [quantity, reason]
properties:
  quantity: {type: integer, format: int64, minimum: 1, maximum: 1000000}
  reason: {type: string, minLength: 3, maxLength: 500}
  reference: {type: string, minLength: 1, maxLength: 120, nullable: true}
