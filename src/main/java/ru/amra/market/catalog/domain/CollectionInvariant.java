package ru.amra.market.catalog.domain;

/** Stable identifiers for editorial collection invariant violations. */
public enum CollectionInvariant {
    INVALID_SLUG,
    INVALID_CONTENT,
    INVALID_ORDER,
    DUPLICATE_PRODUCT,
    INVALID_VERSION
}
