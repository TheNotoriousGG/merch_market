description: A request or business rate limit was exceeded.
headers:
  Retry-After:
    $ref: ../headers/retry-after.yaml
content:
  application/problem+json:
    schema:
      $ref: ../schemas/problem-details.yaml
