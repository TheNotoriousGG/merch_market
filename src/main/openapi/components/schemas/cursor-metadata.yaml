type: object
description: Metadata for keyset pagination of large or mutable feeds.
required:
  - hasNext
properties:
  nextCursor:
    type: string
    minLength: 1
    description: Opaque cursor; clients must not parse or construct it.
  hasNext:
    type: boolean
