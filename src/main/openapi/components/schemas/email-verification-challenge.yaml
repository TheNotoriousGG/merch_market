type: object
required: [challengeId, email, expiresInSeconds]
properties:
  challengeId: {type: string, format: uuid}
  email: {type: string, format: email}
  expiresInSeconds: {type: integer, format: int64, minimum: 1}
  developmentCode: {type: string, nullable: true, pattern: '^[0-9]{6}$'}
